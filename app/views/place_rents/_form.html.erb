<%= form_for [@parking, @place_rent] do |f| %>
  <% if @place_rent.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@place_rent.errors.count, "error") %> prohibited
      this place_rent from being saved:</h2>
    <ul>
    <% @place_rent.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <p>
    <%= f.label :start_date %><br>
    <%= f.datetime_select :start_date %>
  </p>

  <p>
    <%= f.label :end_date %><br>
    <%= f.datetime_select :end_date %>
  </p>

  <p>
    <%= f.label :model %><br>
    <%= collection_select(:place_rent, :car_id, current_person.cars, :id, :model) %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>
